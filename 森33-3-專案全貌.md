# 森33-3行旅 — 專案全貌（給新 AI 的交接文件）

> 建立時間：2026-07-30
> 目的：讓接手這個專案的 AI 能快速補齊所有背景知識，不需從零摸索。

---

## 一、這是什麼專案？

**森33-3行旅** 是南投埔里的一間包棟民宿。每天只接一組客人，整棟獨享。品牌概念是「借住朋友的山邊房子」，不是旅館。

這個 repo 是民宿的**官方形象網站**（獨立站），純展示與導流用途。真正的訂房功能在 BV Trip 平台上處理，官網只需做一個「立即訂房」按鈕連過去。

- **官網網址**：`sen33-3.pages.dev`
- **訂房平台**：`a0911382525.bvtrip.tw/booking`
- **GitHub**：`ggro4444/sen33-3`

---

## 二、技術架構

| 項目 | 技術 |
|------|------|
| 前端框架 | React 19.2 |
| 建置工具 | Vite 8.1 |
| 語言 | TypeScript 7.0 |
| CSS 框架 | Tailwind CSS 3.4（build-time，非 CDN） |
| 動畫 | Framer Motion 12.4 |
| 圖示 | Lucide React 1.25 |
| 路由 | React Router 7.18（**HashRouter**） |
| 部署 | Cloudflare Pages（`sen33-3.pages.dev`） |
| 字體 | Google Fonts：Noto Serif TC（標題）+ Klee One（手寫區） |

### 目錄結構

```
sen33-3/
├── frontend/
│   ├── App.tsx              # 路由設定（HashRouter，4 頁）
│   ├── index.tsx            # 進入點
│   ├── index.html           # HTML 模板（含字體、SEO meta、fallback CSS）
│   ├── index.css            # Tailwind directives
│   ├── constants.ts         # **所有事實資料的唯一來源**（房間、設施、FAQ、景點、聯絡資訊）
│   ├── types.ts             # TypeScript 型別定義
│   ├── tailwind.config.js   # 色彩系統、自訂陰影
│   ├── vite.config.ts       # Vite 設定
│   ├── postcss.config.js
│   ├── components/
│   │   ├── Layout.tsx       # 導航列 + 頁尾（含聯絡資訊、社群連結）
│   │   └── AnimatedSection.tsx  # 滾動淡入動畫 wrapper
│   ├── pages/
│   │   ├── Home.tsx         # 首頁：Hero + Betty 的信 + 設施 + 周邊景點 + Google Maps
│   │   ├── Rooms.tsx        # 房型展示
│   │   ├── Rules.tsx        # 住宿須知（押金、入住退房、環境規範、取消政策）
│   │   └── FAQ.tsx          # 常見問答
│   ├── public/              # 靜態資源（所有圖片均為 WebP）
│   │   ├── hero.webp
│   │   ├── logo-black.png / logo-white.png
│   │   ├── facility-*.webp  # 5 張設施照片
│   │   └── room-*.webp      # 12 張房間照片（每房 3 張）
│   └── dist/                # 建置產出（部署到 Cloudflare Pages）
├── backend/                 # Google App Builder 殘留，已不使用
├── package.json             # Root（幾乎為空）
└── README.md
```

### 重要：backend/ 目錄

這個目錄是 Google App Builder 匯出時殘留的，**已不使用**。官網是純靜態前端，不需要 Node.js 後端。不要花時間在 backend/ 上。

---

## 三、設計系統

### 色彩

| Token | 色碼 | 用途 |
|-------|------|------|
| `warm-bg` | `#FDF5E6` | 頁面背景（暖奶油色） |
| `warm-text` | `#6B4226` | 主要文字（深棕色） |
| `warm-gold` | `#C4A35A` | 強調色、裝飾線、按鈕 |
| `warm-sage` | `#9CAF88` | 輔助色（少用） |
| `warm-dark` | `#2C1E16` | 最深色（少用） |

### 字體

- **標題**：`Noto Serif TC`（Google Fonts，weight 400/600/700）
  - CSS class：`font-serif`、`font-handwriting`
  - 有 fallback 鏈：`'Noto Serif TC', 'SerifFallback', serif`
  - `SerifFallback` 是 `@font-face` 定義的 local() 系統字體（Songti TC / PMingLiU）
- **手寫區**：`Klee One`（Google Fonts，weight 400/600）
  - CSS class：`font-handwriting`
- **內文**：系統 sans-serif（PingFang TC / Microsoft JhengHei）

### 字體策略關鍵教訓

這是最容易踩的坑。歷史教訓：
1. **Google Fonts 中文字體每個 weight 約 9-10MB**，在手機 4G 載入很慢。目前的策略是：系統字體放第一層，Google Fonts 作為增強。字體載入失敗時頁面仍然可讀。
2. **不要直接移除 Google Fonts**：之前嘗試過完全改用系統字體，但視覺質感差很多。目前的雙層策略是經過多次迭代的平衡點。
3. **`font-light` + 低透明度 = 看不見**：民宿主要客群 50-70 歲，`font-light` 在暖色背景上透明度 40% 幾乎隱形。目前已經修復這個問題。

### 自訂陰影

```js
shadow: {
  'warm': '...',     // 中等深度
  'warm-sm': '...',  // 輕微
  'warm-lg': '...',  // 較深
  'card': '...',     // 卡片（含 border 模擬）
  'btn': '...',      // 按鈕預設
  'btn-hover': '...' // 按鈕 hover
}
```

---

## 四、頁面結構與路由

使用 `HashRouter`（`#/` 路由），共 4 頁：

| 路徑 | 頁面 | 內容 |
|------|------|------|
| `#/` | Home | Hero 大圖 → Betty 的歡迎信（手機可展開）→ 設施展示 → 周邊景點 + Google Maps |
| `#/rooms` | Rooms | 4 間房的詳細展示（圖片輪播 + 規格） |
| `#/rules` | Rules | 押金與付款 → 入住退房 → 環境規範 → 取消政策（5 階表格） |
| `#/faq` | FAQ | 11 題常見問答（accordion 展開） |

### HashRouter 的影響

- **SEO 劣勢**：HashRouter 的 URL（`#/rooms`）對搜尋引擎不友好。已有對應的 SEO 方案（見 skills），但尚未實作。
- **優點**：SPA 部署到 Cloudflare Pages 不需設定 redirect 規則，直接能用。

---

## 五、內容資料（事實來源）

**所有事實性資料的唯一來源是 `frontend/constants.ts`**。修改任何房間規格、設施描述、FAQ、聯絡資訊、取消政策等，一律改這個檔案，不要在其他 component 裡硬編碼。

### 房間配置（重要 — 容易寫錯）

| 房間 | 人數 | 床型 | 衛浴 | 陽台 |
|------|:--:|------|:--:|:--:|
| 1F 雙人房 | 2 | 1 加大雙人床 | 無 | 無 |
| 2F 雙人房 | 2 | 1 加大雙人床 | 有（乾濕分離） | 無 |
| 2F 三人房 | 3 | **3 加大單人床** | 有 | 有 |
| 3F 五人房 | 5 | **5 加大單人床** | 有 | 大陽臺 |

**常見錯誤**：把三人房和五人房的床寫成雙人床、漏寫三人房和五人房也有獨立衛浴。

### 關鍵營運數據

| 項目 | 值 |
|------|-----|
| 入住時間 | 15:00 起 |
| 最晚入住 | 22:00 |
| 退房時間 | 11:00 前 |
| 入屋方式 | QR Code 掃碼（LINE 傳送） |
| 押金 | $5,000，退房後 3 天內退還 |
| 延遲退房 | $600 / 30 分鐘 |
| 停車 | 4 台，靠圍牆停放 |
| 人數上限 | 12 人（備品 12 份） |
| 禁菸 | 全室內 + 陽臺 |
| 禁止烤肉 | 全區（含庭院） |
| 不接待寵物 | 是 |
| 不供早餐 | 是 |
| 一次性備品 | 不主動提供 |

### 取消政策（5 階）

| 時間 | 退款 |
|------|:--:|
| 14 天前 | 100% |
| 7-13 天前 | 70% |
| 3-6 天前 | 50% |
| 1-2 天前 | 30% |
| 當天／未通知 | 0% |

天災（政府停班停課）：全額退款或保留訂金延期 3 個月。

---

## 六、圖片資源

所有圖片已壓縮為 WebP 格式，放在 `frontend/public/`。

- Hero 圖：1920px 寬，WebP
- 設施圖：5 張（客廳、廚房、KTV、麻將、庭院），4:5 比例
- 房間圖：每房 3 張，共 12 張，4:3 比例
- Logo：PNG（黑底用白色版、白底用黑色版）

圖片命名規則對應設施/房型名稱，數字標順序。修改圖片時保持此命名慣例。

---

## 七、BV Trip 訂房平台

BV Trip（`manage.bvtrip.tw`）是民宿的訂房管理後台。官網只做展示，**定價和方案全部由 BV Trip 處理**，避免兩邊維護不同步。

### 兩套獨立系統（不要搞混）

| 系統 | 網址 | 用途 |
|------|------|------|
| 管理後台 | `manage.bvtrip.tw` | 設定旅店資訊、房型、價格、方案 |
| 首頁編輯器 | `a0911382525.bvtrip.tw/home/edit` | 區塊式視覺編輯器（banner、文字、圖片） |

- 官網的訂房按鈕 → 連結到 `a0911382525.bvtrip.tw/booking`
- BV Trip 後台登入需要 CSRF token + session cookie + 繞過 reCAPTCHA
- BV Trip 圖片上傳有許多坑：會 strip alpha channel、會強制 resize 到 1920px、favicon 也會被放大

### 包棟方案（在 BV Trip 上設定）

| 方案 | 開放房間 | 人數上限 | 限制 |
|------|---------|:--:|------|
| 12人全包 | 全部 4 房 | 12 | 無 |
| 溫馨6-8人 | 1F雙人 + 2F雙人 + 2F三人 | 8 | 平日限定 |
| 輕奢4人 | 1F雙人 + 2F雙人 | 4 | 平日限定 |

---

## 八、部署

- **平台**：Cloudflare Pages
- **綁定 GitHub repo**：`ggro4444/sen33-3`
- **建置指令**：`cd frontend && npm install && npm run build`
- **建置產出目錄**：`frontend/dist`
- **網域**：`sen33-3.pages.dev`

部署是自動的：push 到 GitHub → Cloudflare Pages 自動建置部署。

---

## 九、已知問題與設計決策

### 已知限制

1. **HashRouter SEO 差**：搜尋引擎不太 index HashRouter 頁面。有 SEO 方案（OG 標籤、JSON-LD、sitemap）在 skill reference 裡但尚未實作。
2. **中文字體載入慢**：Google Fonts 中文字體檔很大（9-10MB/weight），手機 4G 下載入慢。目前用系統字體 fallback 策略。
3. **無後端**：純靜態網站，沒有 CMS。修改內容需要改 code 重新部署。

### 重要設計決策

1. **字體不直接移除 Google Fonts**：試過完全改用系統字體（Songti TC），視覺質感下降太多。目前保留 Google Fonts + fallback。
2. **定價不在官網顯示**：避免官網和 BV Trip 兩邊不同步，所有價格統一在 BV Trip 處理。
3. **HashRouter 而非 BrowserRouter**：Cloudflare Pages 靜態部署下 HashRouter 不需設定 redirect 規則。
4. **React.lazy code splitting**：每個頁面獨立 chunk，按需載入。
5. **Tailwind build-time 而非 CDN**：原本用 CDN（`cdn.tailwindcss.com`），已遷移到 build-time，減少 runtime overhead。

### 手機版注意事項

- **中文字斷行**：中文沒有 word-boundary，長標題在手機上會在任意字元處斷行。需手動加 `<br className="md:hidden" />` 控制斷點。
- **h-screen 在 iOS Safari 有問題**：100vh 包含瀏覽器浮動網址列，hero 區會超出可視範圍。目前用內部間距調整處理，而非 `h-dvh`。
- **至少 375px 寬度測試**每個標題。

---

## 十、修改此專案的工作流程

### 改內容（房型、FAQ、政策等）
→ 改 `frontend/constants.ts`（唯一事實來源）
→ `npm run build` 檢查
→ Git commit + push → Cloudflare Pages 自動部署

### 改設計（樣式、排版、動畫）
→ 改對應 component
→ `npm run dev` 本地預覽
→ `npm run build` 確認 build 成功
→ commit + push

### 改圖片
→ 新圖片放 `frontend/public/`
→ 更新 `constants.ts` 中的圖片路徑
→ 圖片規格：WebP 格式，設施 4:5 比例，房間 4:3 比例

### 注意事項
- **不要改 `backend/` 目錄**：已廢棄
- **不要重新引入 Tailwind CDN**：目前用 build-time
- **不要改字體策略**：已經過多次迭代才穩定
- **改中文內容後**：一定要檢查 UTF-8 編碼（Windows PowerShell 顯示亂碼不代表檔案壞掉）
- **不要用 emoji**：使用者不喜歡在文字內容中出現 emoji

---

## 十一、相關資源

- BV Trip 管理後台：`manage.bvtrip.tw`
- BV Trip 首頁編輯器：`a0911382525.bvtrip.tw/home/edit`
- 官方 LINE：`@469pofut`
- Facebook：`facebook.com/profile.php?id=61576922006701`
- Instagram：`instagram.com/sen333mori`
- Google Maps：搜尋「森33-3行旅」
